#!/bin/sh
set -e

repo="$PREFIX/source/hugo"
if [ -d "$repo" ]; then
  cd "$repo"
  git fetch origin
else
  git clone https://github.com/gohugoio/hugo.git "$repo"
fi
cd "$repo"
go install --tags extended
